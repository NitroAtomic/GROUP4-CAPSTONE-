DB_HOST=localhost
DB_USER=seaware
DB_PASSWORD=change-this-password
DB_NAME=awareness_platform
JWT_SECRET=change-this-to-a-long-random-string
PORT=3000

# FR-20: Premium account email OTP (two-factor login)
# Leave blank to run in dev mode -- OTP codes print to the server console
# instead of being emailed, so the flow is testable with zero setup.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
